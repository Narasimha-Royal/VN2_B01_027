create table company(
id serial primary key not null,
name varchar(30),
age int,
address varchar(50),
salary int
)

select * from company
drop table company
 
create table details(
 stu_id int not null,
 entry_date varchar(130)
 )
  select * from details
  drop table details
  create trigger example after insert on company for each row execute procedure examplefun();
  create or replace function examplefun() returns trigger as $$
  begin 
	  	insert into details (stu_id,entry_date)
	  	values (new.id,current_timestamp);
	  return new;
	 end;
	$$ language plpgsql;
drop trigger example
insert into company (name,age,address,salary)
values ('kumar',27,'chittore',30000)
 